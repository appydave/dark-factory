Analyze the transcript abridgement and extract any important statistics, numbers, percentages, or counts mentioned.

Transcript Abridgement:
{{transcriptAbridgement}}

Return ONLY a JSON object: { "statistics": "..." } — a string summarizing the important numbers found. Use an empty string if none are found. No explanation, no markdown wrapper.
