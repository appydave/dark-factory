# Idea Register — Novel Ideas Worth Keeping (v2 harvest)

**Purpose**: Ideas from the corpus that aren't tickets yet (or are parked) but shouldn't be lost. Grouped by theme; each cites source date(s).

**For Agents**: These feed the dark-factory app-pipeline intake and future brain curation. Recurrence count noted where an idea was restated — recurrence = conviction signal.

---

## Monetization & productization

- **Recipes as sellable products** (06-29, 06-30 ×2 — 3 mentions): extensions/micro-apps become "recipes" (IKEA flat-pack instruction sets, with or without supplied parts) that rebuild a harness-hosted app from a short prompt. Sales channels: community + paywall. Backed by an app/port registry and pub/sub host-extension contracts.
- **Extension recipes as regeneration insurance** (06-30): every extension ships with a recipe that can rebuild it from scratch — the artifact IS the product.
- **Session→marketing asset router** (07-09): post-process coding sessions into tweets, YouTube material, lead magnets (md/PDF); staged human refinement; routed to YouTube/X/Skool.
- **FDE retainer as the client shape** (07-07, 07-13): weekly 90-min training + N custom micro-tools/month, Claude-centric — now live in two pitches (Challenge DV $2,750; Achieve variant). This is becoming David's standard offer — candidate for creator-os/brand-dave codification.
- **Pre-baked micro-apps for small business verticals** (07-11): configurable till/pricing-catalog extensions (juice shop, nail salon, eyebrow tattoo) feeding websites, videos, agents — the Joy-business template generalized.

## Agent architecture & verification

- **Smart contracts as eval rubrics** (07-07 ×2, 07-09 — 3 mentions, two independent sources): verifiable contract + data-backed rubric (security/stability/simplicity/speed) at end of each unit of work; agent A/B scoring with staking; post-hoc verification plans reading whole-repo history beat pre-set plans.
- **Dual-loop / north-star agents** (07-07): give agents north stars rather than prescribing steps; pairs with Anthropic's "unknown unknowns" pattern.
- **Two-agent feedback loop** (07-13): builder agent hits platform gaps → writes prioritized tickets → platform agent extends the platform. Requires a platform roadmap to triage against.
- **Doc-watcher agent pattern** (07-06 ×2): an agent whose ONLY job is watching documentation repos and reporting change — product knowledge stays in project docs, agent brain is just the searchable index.
- **Connector-first over hidden in-app memory** (07-10): connectors to your own specialized storage/brains beat vendor in-app memory — data sovereignty, no lock-in.
- **Context-aware self-learning utility skills** (07-10): skills that adapt to conversation subject + user state (time-of-day fatigue) — same family as the existing `buggered` skill; cites David's RN rename + BTW prompt-drafting habits.
- **Chaperone/observer for long-running orchestrators** (07-09): main factory conversation needs a watcher — inside vs outside trade-off. (Existing `appydave:chaperone` skill is a candidate implementation.)

## Capture & knowledge pipeline

- **Capture intelligence hub** (07-11): self-sent emails carrying article/YouTube links auto-processed into a common "transcript" form → summarize + ontology-tag. CL beyond wearables.
- **Quest/side-quest linkage for captures** (07-13 ×2): temporal chains + one-capture-splits-into-many-ideas — imported from Dark Factory's quest structure into knowledge capture.
- **Action-attach on transcripts via browsing audit-log** (07-13): accumulate wants/ideas while browsing, consolidate in one pass — a "review session" interaction model rather than per-item CRUD.
- **Vernacular-normalization prompt** (07-12): fix transcription of personal terms without altering context — a personal-dictionary layer any transcript pipeline can share (CL, video pipeline, meeting notes).
- **OMI vs Plaud bake-off method** (07-06): same micro-conversations on both devices as a controlled quality experiment — reusable pattern for any capture-device comparison.
- **Ideas-not-just-lessons extraction** (07-02): upgrade Lisa/KDD to extract IDEAS from projects into provenance-chained small PRs — the knowledge system currently only harvests lessons/patterns.

## Tooling & automation

- **Electron injection for programmatic control** (07-11 ×2): unobfuscated Electron apps (Gling) can host an injected MCP/API layer at startup for remote control — generalizable to other desktop tools David rents but hates driving manually.
- **Prompt-batch TUI revival** (06-30): the old cron-paced batch runner (appydave-tools era) as the executor for large prompt matrices — throttled, unattended.
- **Shortlisting mechanism for generative variation matrices** (06-30): ~100 axis-combinations → 10–20 candidates (YouTube takes 3) — the hard problem in thumbnail generation isn't generation, it's culling.
- **Skill registry with staleness + learnings** (07-04): investigating (~1,100) vs in-use (~100); per-skill learnings/metadata travel WITH the skill; taxonomy search; detail viewer.
- **Visual audit via Loom + ffmpeg + Qwen** (07-09): frame-extract a screen recording and let a vision model audit UI state — offered to Tony for manual-testing bottleneck; reusable for David's own UAT (pairs with Taylor UAT-agent + Playwright pattern).
- **Mochaccino schema→image visualization** (07-02): render app/ticket/side-quest state as generated images from schema+data — ambient factory visibility.

## Content & video

- **Joy video pipeline pattern** (07-11): Whisper transcript + Qwen visual tagging of B-roll → auto-assembled bilingual videos; HeyGen avatars ONLY for obviously-non-human use. The demonstrated client-demo asset (Challenge DV, meetups).
- **Brand-configured generation** (06-30): per-brand rule docs (aesthetics, ghost text, field sets) as the config layer any generative tool reads — thumbnail first, generalizes to all branded output.

## Community / market intel worth acting on

- Smart-contracts rubric idea came from OUTSIDE (Tony, peer session) and stuck across 3 captures — meetups are functioning as an R&D input channel; the ai-meetups→idea-register pipeline deserves to be deliberate.
- Platform 5.6 regression report (07-10): recheck vendor in 1–4 weeks before any dependency decision.
