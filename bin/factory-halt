#!/usr/bin/env bash
# factory-halt — thin wrapper over engine/halt.py halt.
# Usage: bin/factory-halt ["reason"] [--by <name>]
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec python3 "$DIR/engine/halt.py" halt "$@"
